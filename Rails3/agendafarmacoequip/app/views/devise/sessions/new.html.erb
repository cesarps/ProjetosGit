<h2>Entrar</h2>

<%= form_for(resource, :as => resource_name, :url => session_path(resource_name)) do |f| %>
  <div><%= f.label :email %><br />
  <%= f.email_field :email %></div>

  <div><%= f.label :senha %><br />
  <%= f.password_field :password %></div>

  <% if devise_mapping.rememberable? -%>
    <div><%= f.check_box :remember_me %> <%= f.label :lembrar %></div>
  <% end -%>

  <div><%= f.submit "Entrar" %></div>
<% end %>

<%= render :partial => "devise/shared/links" %>

<!--< %= link_to "Entrar como usuario comum", new_session_path(resource_name) %> -->



<%= link_to "Entrar como usuario comum", login_devise_path %> <br />
<%= link_to 'Entrar como administrador', admin_session_path    %>