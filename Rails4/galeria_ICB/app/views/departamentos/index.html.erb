<h1>Departamentos</h1>

<table class="table table-condensed">
  <thead>
    <tr>
      <th>Nome</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @departamentos.each do |departamento| %>
      <tr>
        <td><%= departamento.nome %></td>
        <td><%= link_to 'Mostrar', departamento, class: 'btn btn-default' %></td>
        <td><%= link_to 'Editar', edit_departamento_path(departamento), class: 'btn btn-primary active' %></td>
        <td><%= link_to 'Apagar', departamento, method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn btn-danger active' %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Novo Departamento', new_departamento_path, class: 'btn btn-primary active' %>